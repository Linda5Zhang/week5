<%= form_for(@venue) do |f| %>

  <% if @venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue.errors.count, "error") %> prohibited this event from being saved:
      </h2>

      <ul>
      <% @venue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Name' %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label 'Address' %><br />
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
<% end %>
